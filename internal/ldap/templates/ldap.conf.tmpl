# /etc/openldap/ldap.conf — generated by clustr-serverd for node {{.Hostname}}
# DO NOT EDIT — managed by clustr.
#
# This file configures OpenLDAP client tools (ldapsearch, ldapwhoami, etc.)
# for LDAP utilities on the node. sssd has its own config in /etc/sssd/sssd.conf.

URI     {{.ServerURI}}
BASE    {{.BaseDN}}
TLS_CACERT /etc/openldap/certs/clustr-ca.pem
TLS_REQCERT demand
