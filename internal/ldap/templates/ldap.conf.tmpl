# /etc/openldap/ldap.conf — generated by clonr-serverd for node {{.Hostname}}
# DO NOT EDIT — managed by clonr.
#
# This file configures OpenLDAP client tools (ldapsearch, ldapwhoami, etc.)
# for LDAP utilities on the node. sssd has its own config in /etc/sssd/sssd.conf.

URI     {{.ServerURI}}
BASE    {{.BaseDN}}
TLS_CACERT /etc/openldap/certs/clonr-ca.pem
TLS_REQCERT demand
